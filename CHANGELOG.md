# Changelog

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循
[语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

## [2.0.0] - 2026-08-03

### Added

- 重要日期支持农历生日、闰月标记和普通月补过/仅闰月两种规则；工作区升级至 schema 3，并保持旧数据可读。
- 系统日历导入、来源关联、手动同步和本地事件导出；系统日历来源的标题、日期、重复规则由 EventKit 保持一致。
- 控制中心右上角设置、开机自启、可录制的全局快捷键（默认 `⌥⌘N`），以及当前屏幕顶部中央的新建快速便签。
- 快速便签可单独置顶或取消置顶；置顶状态重启后保留。
- `scripts/build-dmg.sh` 生成并校验 universal DMG、ad-hoc 签名及 SHA-256；版本标签工作流自动发布安装包。

### Changed

- 重要日期编辑器增加公历/农历切换与系统日历工作流；农历转换保持在纯 Foundation 核心层。
- 核心模型与持久化契约自检扩展至 50 项，涵盖闰月补过、日历关联与置顶兼容性。

### Security

- 日历权限仅在用户主动导入、关联或同步时请求；日历数据始终经本机 EventKit 处理。

## [1.1.1] - 2026-08-02

### Fixed

- 修复斜体小数倾斜值的检测，避免再次点击时重复应用而无法取消。
- 将视觉很弱的倾斜属性替换为可逆的字体矩阵剪切，让中文字形明显右倾，同时保持 RTF 重启兼容。
- 修复圆角系统字体无法取消粗体的问题，再次点击后恢复常规字重。
- 将不明确的颜色圆点替换为带文字标识和当前色块的调色板按钮。
- 增加 AppKit 控制器回归测试，覆盖选区范围、状态、反复切换、样式组合、颜色和 RTF 持久化。
- CI 生成工程时固定为 Xcode 15 兼容格式。

## [1.1.0] - 2026-08-02

### Added

- 便签增加富文本工具栏，支持对选中文字设置颜色、粗体、斜体、下划线和删除线。
- 支持清除选中文字的格式；未选择文字时，格式会应用到随后输入的内容。
- 富文本以 RTF 数据保存在原有本地工作区中，同时保留纯文本字段兼容旧版便签。

### Changed

- 便签编辑器由 SwiftUI `TextEditor` 升级为 AppKit `NSTextView`，保留直接编辑、撤销、滚动和自动保存体验。
- 核心模型与持久化自检增加到 40 项。

## [1.0.0] - 2026-08-02

### Fixed

- 移除隐藏标题栏仍占用的顶部安全区，卡片可贴近菜单栏并与系统桌面小组件顶部对齐。
- 新建卡片和恢复默认位置时采用更接近系统小组件的顶部间距。
- 将卡片提升到桌面图标层上方并接受 first mouse，关闭控制中心后仍可单击进入编辑。
- 增加小号方形、中号横向、大号方形尺寸预设，同时保留自由缩放。
- 便签空白占位文字与输入光标改为同一文本基线，消除上下错位。
- 卡片取得编辑焦点时继续固定在桌面层，不再临时提升并遮挡普通应用窗口。

### Added

- 可自由移动、缩放并直接编辑的便签与倒数日桌面卡片。
- 待办卡片，支持新增、编辑、完成、删除、优先级与到期日。
- 菜单栏入口和卡片控制中心。
- App Sandbox 内的版本化 JSON 工作区、原子保存和损坏文件备份。
- 窗口 frame、屏幕标识、显示状态与卡片主题持久化。
- `TinyDeskSelfTests` 核心模型与持久化契约自检。
- 每张卡片可独立切换毛玻璃、透明、不透明背景，以及石墨、暖沙、薄荷、玫瑰、海洋颜色。
- 待办支持全部、未完成、已完成筛选；新事项默认计划为当天，跨日后显示“昨日未完成”或逾期天数。
- 重要日期日历/列表双视图，支持生日、纪念日、节日、其他分类，以及置顶和分类筛选。
- 重要日期支持一次性/每年重复、起始年份、年龄/周年数，以及 2 月 29 日在非闰年的提醒规则。
- 重要日期支持当天或提前 1/3/7 天的本地通知，可选择提醒时间；应用在前台时也会显示横幅。
- 每张桌面卡片可独立锁定位置；锁定状态持久化，并可从卡片或控制中心解除。

### Changed

- 已完成待办显示删除线，并在“全部”列表中稳定移动到未完成事项之后。
- 倒数日模块升级为全局重要日期库；旧倒数日会通过 schema 2 自动迁移为一次性事件。
- 重要日期卡片移除标题拖动区，将日历/列表、分类筛选和新增操作整合到第一行；外观与尺寸收进设置菜单。
- 工程由多扩展架构迁移为单一 SwiftUI + AppKit macOS 应用。
- 产品范围聚焦便签、重要日期和待办，形成首个公开版本 1.0.0。

### Removed

- 所有系统扩展、联网聚合以及跨进程共享能力。

[Unreleased]: https://github.com/xassuyge003-ui/TinyDesk/compare/v2.0.0...HEAD
[2.0.0]: https://github.com/xassuyge003-ui/TinyDesk/compare/v1.1.1...v2.0.0
[1.1.1]: https://github.com/xassuyge003-ui/TinyDesk/compare/v1.1.0...v1.1.1
[1.1.0]: https://github.com/xassuyge003-ui/TinyDesk/compare/v1.0.0...v1.1.0
[1.0.0]: https://github.com/xassuyge003-ui/TinyDesk/releases/tag/v1.0.0
